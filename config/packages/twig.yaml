twig:
    file_name_pattern: '*.twig'
    date:
        timezone: '%app.default_timezone%'
    globals:
        app_name: '%app.name%'
        app_version: '%app.version%'

when@test:
    twig:
        strict_variables: true
