/** Initialisations JS propres à la page d'accueil (aucun accordéon : FAQ et témoignages sont statiques). */
export const registerHomePage = () => {
  if (!document.getElementById('hero')) {
    return;
  }
};
