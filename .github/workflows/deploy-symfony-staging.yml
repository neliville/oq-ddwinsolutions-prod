# DEPRECATED — Déploiement Azure staging désactivé.
# Staging / prod : o2switch via SSH → scripts/pre-deploy-backup-o2switch.sh puis ./deploy.sh
# Voir DEPLOY_O2SWITCH.md et docs/DEPLOY_RUNBOOK_PREMIUM_RELEASE.md
name: (deprecated) Deploy Symfony to Azure Staging

on:
  workflow_dispatch:

jobs:
  deprecated-notice:
    name: Workflow désactivé (Azure)
    runs-on: ubuntu-latest
    steps:
      - name: Information
        run: |
          echo "::notice title=Workflow déprécié::Le déploiement Azure staging n'est plus utilisé."
          echo "Déploiement actuel : o2switch (SSH) — ./deploy.sh"
          echo "Documentation : DEPLOY_O2SWITCH.md, docs/DEPLOY_RUNBOOK_PREMIUM_RELEASE.md"
