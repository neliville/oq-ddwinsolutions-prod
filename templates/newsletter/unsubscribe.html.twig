{% extends 'base.html.twig' %}

{% block title %}Désabonnement à la newsletter | OUTILS-QUALITÉ{% endblock %}

{% block meta_description %}Désabonnez-vous de notre newsletter. Votre avis nous aide à améliorer nos contenus.{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm">
                <div class="card-body p-4 p-md-5">
                    <h1 class="h3 mb-4">Vous êtes bien désinscrit(e)</h1>
                    
                    <div class="alert alert-success mb-4">
                        <i class="fas fa-check-circle me-2"></i>
                        L'adresse <strong>{{ email }}</strong> ne recevra plus nos emails de outils-qualite.com.
                    </div>

                    <p class="mb-4">
                        Pour nous aider à améliorer nos contenus, pouvez-vous nous dire pourquoi vous vous désabonnez&nbsp;?
                    </p>

                    {{ form_start(form, {'attr': {'class': 'unsubscribe-form'}}) }}
                        <div class="mb-4">
                            {{ form_label(form.reasons, null, {'label_attr': {'class': 'form-label fw-semibold mb-3'}}) }}
                            <div class="form-check-group">
                                {% for child in form.reasons %}
                                    <div class="form-check">
                                        {{ form_widget(child, {'attr': {'class': 'form-check-input'}}) }}
                                        {{ form_label(child, null, {'label_attr': {'class': 'form-check-label'}}) }}
                                    </div>
                                {% endfor %}
                            </div>
                            {{ form_errors(form.reasons) }}
                        </div>

                        <div class="mb-4">
                            {{ form_label(form.comment, null, {'label_attr': {'class': 'form-label fw-semibold'}}) }}
                            {{ form_widget(form.comment) }}
                            {{ form_errors(form.comment) }}
                        </div>

                        <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-paper-plane me-2"></i>
                                Envoyer mon retour
                            </button>
                            <button type="submit" name="skip" value="1" class="btn btn-outline-secondary">
                                Passer cette étape
                            </button>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.form-check-group {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
}

.form-check-group .form-check {
    padding: 0.75rem;
    border: 1px solid #dee2e6;
    border-radius: 0.375rem;
    transition: all 0.2s;
}

.form-check-group .form-check:hover {
    background-color: #f8f9fa;
    border-color: #0d6efd;
}

.form-check-group .form-check-input {
    margin-top: 0.25rem;
}

.form-check-group .form-check-label {
    margin-left: 0.5rem;
    cursor: pointer;
}
</style>
{% endblock %}

