{# @block content The close trigger element (e.g., a `Button`) that closes the dialog when clicked #}
{%- set dialog_close_attrs = {
    'data-action': 'click->ux-dialog#close'|html_attr_type('sst'),
} -%}
{%- block content %}{% endblock -%}
