<nav class="main-navbar fixed top-0 left-0 right-0" id="mainNav" aria-label="Navigation principale">
    {# Calque sous la barre : assombrit le contenu quand le menu mobile est ouvert #}
    <div class="navbar-mobile-backdrop" data-navbar-backdrop aria-hidden="true"></div>

    <div class="container main-navbar__bar">

        {# ── Brand ────────────────────────────────────────────────────────── #}
        <a class="main-navbar__brand flex items-center no-underline shrink-0"
           href="{{ path('app_home_index') }}">
            <img src="{{ asset('img/logo.webp') }}" alt="OUTILS-QUALITÉ" width="40" height="40"
                 class="mr-2 shrink-0" loading="eager" fetchpriority="high">
            <span class="font-bold main-navbar__brand-text">OUTILS-QUALITÉ</span>
        </a>

        {# ── Mobile : sidebar toggle entre le brand et le hamburger ──────── #}
        {% if app.user %}
        <div class="flex lg:hidden items-center gap-1 ml-auto mr-2">
            {% if show_sidebar_toggle %}
                <button class="navbar-icon-btn" type="button" id="sidebarToggleMobile"
                        aria-label="Afficher la navigation" aria-expanded="true">
                    <i data-lucide="panel-left" width="20" height="20"></i>
                </button>
            {% else %}
                <a class="navbar-icon-btn" href="{{ path('app_dashboard_index') }}"
                   aria-label="Accéder au tableau de bord">
                    <i data-lucide="layout-dashboard" width="20" height="20"></i>
                </a>
            {% endif %}
        </div>
        {% endif %}

        {# ── Hamburger (mobile) ──────────────────────────────────────────── #}
        <button class="navbar-toggler ml-auto shrink-0 inline-flex flex-row items-center justify-center main-navbar__toggler lg:ml-1" type="button"
                data-navbar-toggler
                aria-expanded="false" aria-label="Afficher le menu">
            <span class="navbar-toggler__icons" aria-hidden="true">
                <span class="navbar-toggler__icon navbar-toggler__icon--menu"><i data-lucide="menu" width="20" height="20"></i></span>
                <span class="navbar-toggler__icon navbar-toggler__icon--close"><i data-lucide="x" width="20" height="20"></i></span>
            </span>
        </button>

        {# ── Collapse : liens de navigation + CTA mobile non connecté ────── #}
        <div class="navbar-collapse" id="navbarNav" data-navbar-collapse>
            <div class="navbar-menu flex-1">
                {{ knp_menu_render('app.navbar', {
                    'template': 'menu/navbar.html.twig',
                    'currentClass': 'active',
                    'ancestorClass': 'active',
                    'currentAsLink': true,
                    'allow_safe_labels': true
                }) }}
            </div>

            {% if not app.user %}
            <div class="lg:hidden mt-6 pt-5 border-t border-slate-200/80">
                <a class="inline-flex items-center justify-center gap-2 bg-primary text-primary-foreground px-4 py-3 rounded-md font-medium hover:opacity-90 transition-opacity no-underline w-full max-w-sm mx-auto" href="{{ path('app_login') }}">Connexion</a>
            </div>
            {% endif %}
        </div>

        {# ── Actions droite — desktop (toujours visibles) ─────────────────── #}
        {% if app.user %}
        <div class="navbar-right-actions hidden lg:flex items-center gap-2 ml-auto">

            {# Sidebar toggle #}
            {% if show_sidebar_toggle %}
                <button class="sidebar-toggle-btn" type="button" id="sidebarToggle"
                        aria-label="Afficher la navigation" aria-expanded="true">
                    <i data-lucide="panel-left" width="18" height="18"></i>
                    <span class="sidebar-toggle-label ml-1">Masquer la navigation</span>
                </button>
            {% else %}
                <a class="sidebar-toggle-btn" href="{{ path('app_dashboard_index') }}"
                   aria-label="Accéder au tableau de bord">
                    <i data-lucide="layout-dashboard" width="18" height="18"></i>
                    <span class="sidebar-toggle-label ml-1">Tableau de bord</span>
                </a>
            {% endif %}

            {# Help dropdown #}
            <div class="relative" data-controller="profile-dropdown">
                <button class="navbar-icon-btn" type="button"
                        data-profile-dropdown-target="button"
                        data-action="click->profile-dropdown#toggle"
                        aria-expanded="false" aria-haspopup="true"
                        title="Aide" aria-label="Aide">
                    <i data-lucide="circle-help" width="20" height="20"></i>
                </button>
                <ul class="dropdown-menu dropdown-menu-end navbar-help-menu shadow-sm border"
                    data-profile-dropdown-target="menu"
                    hidden>
                    <li>
                        <a class="dropdown-item"
                           href="{{ path('app_blog_index') }}">
                            <i data-lucide="book-open" width="18" height="18" class="shrink-0" aria-hidden="true"></i>
                            <span>Blog QSE</span>
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item"
                           href="{{ path('app_outils_index') }}">
                            <i data-lucide="layers" width="18" height="18" class="shrink-0" aria-hidden="true"></i>
                            <span>Tous les outils</span>
                        </a>
                    </li>
                    <li><hr class="dropdown-divider my-1"></li>
                    <li>
                        <a class="dropdown-item"
                           href="{{ path('app_contact_index') }}">
                            <i data-lucide="mail" width="18" height="18" class="shrink-0" aria-hidden="true"></i>
                            <span>Nous contacter</span>
                        </a>
                    </li>
                </ul>
            </div>

            {# Profile dropdown — avatar uniquement #}
            <div class="relative" data-controller="profile-dropdown">
                <button class="navbar-icon-btn navbar-avatar-btn p-0" type="button"
                        id="profileDropdown"
                        data-profile-dropdown-target="button"
                        data-action="click->profile-dropdown#toggle keydown.enter->profile-dropdown#toggle keydown.space->profile-dropdown#toggle keydown.arrow-down->profile-dropdown#openWithKeyboard keydown.arrow-up->profile-dropdown#openWithKeyboard"
                        aria-expanded="false" aria-haspopup="true"
                        aria-label="Menu utilisateur">
                    <div class="profile-dropdown__avatar bg-primary text-white rounded-full flex items-center justify-center">
                        {{ app.user.email|slice(0, 1)|upper }}
                    </div>
                </button>
                <ul class="dropdown-menu dropdown-menu-end shadow-lg border-0 mt-2 profile-dropdown__menu"
                    data-profile-dropdown-target="menu"
                    aria-labelledby="profileDropdown"
                    hidden>
                    <li>
                        <div class="px-4 py-3 border-b">
                            <div class="font-semibold truncate" style="max-width:220px;">{{ app.user.email }}</div>
                            <small class="text-muted-foreground">Membre connecté</small>
                        </div>
                    </li>
                    <li>
                        <a class="dropdown-item"
                           href="{{ path('app_profile_index') }}" role="menuitem">
                            <i data-lucide="user" width="18" height="18" class="shrink-0" aria-hidden="true"></i>
                            <span>Mon profil</span>
                        </a>
                    </li>
                    <li>
                        <a class="dropdown-item"
                           href="{{ path('app_creations_index') }}" role="menuitem">
                            <i data-lucide="file-text" width="18" height="18" class="shrink-0" aria-hidden="true"></i>
                            <span>Mes créations</span>
                        </a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') %}
                    <li><hr class="dropdown-divider my-1"></li>
                    <li>
                        <a class="dropdown-item text-primary"
                           href="{{ path('app_admin_dashboard_index') }}" role="menuitem">
                            <i data-lucide="shield" width="18" height="18" class="shrink-0" aria-hidden="true"></i>
                            <span>Administration</span>
                        </a>
                    </li>
                    {% endif %}
                    <li><hr class="dropdown-divider my-1"></li>
                    <li>
                        <a class="dropdown-item text-destructive"
                           href="{{ path('app_logout') }}" role="menuitem">
                            <i data-lucide="log-out" width="18" height="18" class="shrink-0" aria-hidden="true"></i>
                            <span>Déconnexion</span>
                        </a>
                    </li>
                </ul>
            </div>

        </div>
        {% else %}
        <a class="hidden lg:inline-flex items-center justify-center gap-2 bg-primary text-primary-foreground px-3 py-1.5 text-sm rounded-md font-medium hover:opacity-90 transition-opacity no-underline ml-auto"
           href="{{ path('app_login') }}">Connexion</a>
        {% endif %}

    </div>
</nav>
