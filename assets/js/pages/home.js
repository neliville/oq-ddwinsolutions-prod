export const registerHomePage = () => {
  const heroSection = document.getElementById('hero');
  if (!heroSection) {
    return;
  }

  // Exemple d’amélioration future : gestion d’observateurs, animations conditionnelles, etc.
};
